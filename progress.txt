# Progress — Octopus brain/arm split (review feedback applied)

## Feedback addressed
Reviewer identified a structural bug: octopus.md was called "sole builder" but had
no edit/write grants (the validator enforced their absence), and arms were routed
through @autonomous (recursion + no read-only enforcement). Also: committee-review
inflation risk; arms didn't "pay rent"; should default to 3 arms not 8.

## Checklist
- [x] Rewrite agents/octopus.md as the BRAIN: edit/write allow (sole builder), dispatches @octopus-arm (not @autonomous), admission test, default 3 arms (cap 8), 3 rounds
- [x] Create agents/octopus-arm.md: read-only lens (edit/write deny, no task), structured perception with Evidence/Confidence/Actionability/DedupKey
- [x] Update docs/STRATEGY-CONTRACT.md section 5: brain/arm split table, two sensing phases, restraint package, upgraded perception contract, reference impl = both files
- [x] octopus task map: octopus-arm + reviewer (removed autonomous-as-arm-dispatch)
- [x] tests/verify_opencode.py: EXPECTED_AGENT_FILES +octopus-arm.md; EXPECTED_MODES +octopus-arm; octopus rules -> builder posture (edit/write allow, task octopus-arm); new octopus-arm read-only rules block; rewrote A4 check_octopus_perception -> brain/arm split validation; relaxed A2 task-posture to accept coordinator brains (reviewer + deny* + >=1 delegation allow)
- [x] .opencode/strategies.json, README.md, SPEC.md updated for two-agent split
- [x] Run full verification

## Run log
- node --test tests/plugins/*.test.mjs: 48/48 pass.
- verify_opencode.py --skip-llm: A2 "3 active/reference", A4 "brain/arm split: brain builds + dispatches octopus-arm; arm is read-only with evidence contract". Rule counts: @octopus 14, @octopus-arm 10. All agents/modes/rules pass. Only plugin_load fails (pre-existing).

## Restraint package (reviewer's recommendations, all adopted)
- Admission test (5 conditions) in octopus.md before strategy runs.
- Default 3 arms, escalate to 8 only with SPEC justification.
- 3 build->feel->revise rounds cap.
- Arm contract: Evidence, Confidence, Actionability, DedupKey -> each arm "pays rent".

## Latest verification
Command: python3 tests/verify_opencode.py --skip-llm
Only failure: pre-existing plugin_load (requires live OpenCode startup log).

## Deploy note
agents/octopus-arm.md is a NEW file — needs a symlink:
  ./scripts/deploy-opencode-agents.sh install --with-skills --with-plugins
then restart OpenCode.
