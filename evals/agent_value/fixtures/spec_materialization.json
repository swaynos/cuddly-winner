{
  "id": "spec_materialization",
  "title": "Prometheus SPEC payload is materialized before execution",
  "category": "spec_freshness",
  "prompt": "Use the newest visible <spec filename=\"SPEC.md\"> payload, not an old on-disk spec.",
  "expected": {
    "requires_spec_fresh": true,
    "requires_valid_evidence": true,
    "requires_task_success": true
  }
}
