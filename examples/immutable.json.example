{
  "_status": "RESERVED PLACEHOLDER: the current plugin does not load or enforce this file.",
  "_comment": "Future project overrides may narrow managed-agent permissions. Native Plan and Build remain unaffected.",

  "readonly": [
    "<frozen-file.py>"
  ],

  "write_allowlist": {
    "prometheus": ["SPEC.md", ".spike/**"]
  }
}
